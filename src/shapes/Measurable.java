package shapes;

public interface Measurable {

    // 2.Inside of the shapes directory, create a Measurable interface with the following methods:
    double getPerimeter();

    double getArea();

}// interface